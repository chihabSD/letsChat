import React, { useEffect, useState } from "react";

const MainLayoutLoading = ({children}) => {
 
 
  return (
    <div className="mainLoading-container">
     {children}
    </div>
  );
};

export default MainLayoutLoading;
